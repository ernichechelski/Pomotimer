load("//config:custom_rules.bzl", "first_party_library")

licenses(["notice"])

first_party_library(
    name = "SwiftLib",
    test_deps = [
        "//Modules/Carthage:Nimble",
    ],
    deps = [
       "//Modules/Carthage:RxSwift",
    ],
)